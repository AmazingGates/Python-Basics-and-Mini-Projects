# Pip - In this section we will be going over the process of using pip to download packages and modules in python

# Pip is package management for packages and modules from the python package index

# This is a list of pip commands, packages, modules

# help:      pip
# check:     pip --version
# updates:   pip install --upgrade pip
# installed packages:    pip list
# check outdated packages:     pip list --outdated
# install a package:     pip install "package name"